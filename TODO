todo:
- add export command
- source .profile for compress-images
- remove duplicates from paths file

working:
- alias
- git-config
- tool(apt, bash)

done:
- deprecate command => only accepts commands
- missing sudo in command
- $version
- missing tools
- refactor gunzip, unzip and others (remove url)
- $target
- export
- control installed modules
- missing properties like scope in git-config
- locally store modules for future updates comparison
- missing dependencies (unzip, zip) install
